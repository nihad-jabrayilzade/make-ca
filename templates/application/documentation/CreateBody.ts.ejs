import { ApiProperty } from "@nestjs/swagger";

export class RestApiCreate<%= entity.pascalCase %>Body {
  // Add specific properties with ApiProperty decorator for Swagger documentation
  // Example:
  // @ApiProperty({ description: 'The name' })
  // name: string;
} 