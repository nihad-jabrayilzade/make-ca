import { EntityNotFoundException } from "@core/shared/exception";
import { <%= entity.pascalCase %> } from "@core/domain/<%= entity.kebabCase %>/entity";

export class <%= entity.pascalCase %>NotFoundException extends EntityNotFoundException {
  constructor(id: <%= entity.pascalCase %>["id"]) {
    super(`<%= entity.pascalCase %> with id ${id} not found`);
  }
} 