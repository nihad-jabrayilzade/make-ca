import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';

// Import all entities and repositories

@Module({
  imports: [
    TypeOrmModule.forFeature([
      // Add all TypeORM entities here
    ]),
  ],
  providers: [
    // Add all repository providers here
  ],
  exports: [
    // Export all repository providers here
  ],
})
export class TypeOrmPersistenceModule {} 